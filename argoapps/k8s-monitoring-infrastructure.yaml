apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: k8s-monitoring-infrastructure
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/callm23/argocd.git   # dein Git Repo
    targetRevision: main                                # Branch oder Tag
    path: applications/k8s-monitoring-infrastructure                                 # Ordner im Repo
  destination:
    server: https://kubernetes.default.svc
    namespace: grafana
  syncPolicy:
    automated:
      prune: true       # löscht nicht mehr vorhandene Objekte
      selfHeal: true    # korrigiert manuelle Änderungen

